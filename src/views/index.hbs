<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<style>
  img {
    width: 50px;
    height: 50px;
  }
</style>
<body>
  <h1>Главная страница</h1>
  <a href="/create">Добавить</a>

  {{#if productCheck }}
      <ul>
          {{#each products}}
              <li>{{this.title}}</li>
              <p>{{this.image.name}}</p>
              {{!-- <img src="../../resources/static/assets/tmp/{{this.image.name}}" alt=""> --}}
          {{/each}}
        </ul>
  {{/if }}
</body>
</html>